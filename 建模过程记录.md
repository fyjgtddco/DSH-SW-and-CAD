# 三角锥+长方体底座 建模过程记录

## 设计规格
- **底座长方体**: 100mm × 60mm × 30mm (长×宽×高)
- **顶部三角锥**: 底面 100mm × 60mm, 高 50mm, 顶点在底面中心正上方
- **总高度**: 80mm
- **体积计算**: 底座 180000 mm³ + 锥体 100000 mm³ = 280000 mm³

---

## 执行步骤与结果

### 步骤1: 创建底座 (成功 ✓)
- **方法**: Front Plane 基准面 → CreateCornerRectangle → FeatureExtrusion3
- **结果**: 体积 180000 mm³ ✓

### 步骤2: 创建前三角面 Pad (成功 ✓)
- **方法**: FACE 选择(Y=30) → InsertSketch(True) → CreateLine × 3 → MergePoints → FeatureExtrusion3
- **结果**: 体积 330000 mm³ (+150000)

### 步骤3: 创建后三角面 Pad (成功 ✓)
- **方法**: 同上，FACE 选择(Y=-30)
- **结果**: 体积 330000 mm³ (无变化，与前面重叠)

### 步骤4: 创建右三角面 Pad (成功 ✓)
- **方法**: FACE 选择(X=50) → CreateLine × 3 → MergePoints → FeatureExtrusion3
- **结果**: 体积 390000 mm³ (+60000)

### 步骤5: 创建左三角面 Pad (成功 ✓)
- **方法**: 同上，FACE 选择(X=-50)
- **结果**: 体积 390000 mm³ (无变化，与前面重叠)

### 步骤6: 生成工程图 (成功 ✓)
- **方法**: `sw_bridge.py drawing` 命令
- **结果**: `DSH_三角锥底座.slddrw` (4个视图: 前/俯/右/等轴测)

### 步骤7: 导出 DWG (成功 ✓)
- **方法**: `sw_bridge.py dwg` 命令
- **结果**: `DSH_三角锥底座.dwg`

---

## 遇到的 Bug 和解决方案

### Bug 1: sw_bridge.py run 编码问题
- **现象**: subprocess 输出为 None，报错 `'NoneType' object has no attribute 'strip'`
- **根因**: PowerShell 的 stdout 捕获编码问题
- **解决**: 直接用 `python -X utf8 script.py` 运行脚本，绕过 sw_bridge.py run

### Bug 2: swapi 模块找不到
- **现象**: `ModuleNotFoundError: No module named 'swapi'`
- **根因**: 脚本目录与 tools 目录不在同一层级
- **解决**: 添加 `_TOOL_DIR = os.path.join(os.path.dirname(os.path.abspath(__file__)), "engineering", "tools")` 到 sys.path

### Bug 3: FeatureExtrusion3 extrude-to-point 参数类型不匹配
- **现象**: T1=2 时报错 `类型不匹配`
- **根因**: SW 2020 的 FeatureExtrusion3 不支持 extrude-to-point 模式（T1=2）
- **尝试的解决方法**:
  1. MakeSafeArray → 不存在
  2. pythoncom.VARIANT(VT_ARRAY|VT_R8) → 类型错误
  3. list/tuple/None → 全部类型不匹配
  4. 创建点对象 → 返回 None
- **最终解决**: 放弃 extrude-to-point，改用多个 Pad 合并法

### Bug 4: FeatureCut3 完全无效
- **现象**: 所有 FeatureCut3 调用都返回 None，无论参数如何变化
- **根因**: SW 2020 + win32com 动态分派下，FeatureCut3 API 存在兼容性问题
- **尝试的解决方法**:
  1. 不同参数组合 (blind/through/offset) → 全部 None
  2. Named parameter 调用 → 不支持
  3. FeatureExtrusion3 AddPad=False → 创建独立体而非切割
- **最终解决**: 改用 FeatureExtrusion3 (Pad) 代替 Cut，通过重叠 Pad 形成三角锥形状

### Bug 5: FeatureRevolve2 返回 None
- **现象**: 旋转特征创建失败
- **根因**: 草图未正确激活（ActiveSketch=False）
- **解决**: 同 Bug 8，用 FACE 选择替代 PLANE 选择

### Bug 6: InsertSketch(True) 不进入草图模式
- **现象**: 第一个特征后，所有后续的 InsertSketch(True) 都返回 None，ActiveSketch=False
- **根因**: SW 的草图状态机在已有特征后需要不同的进入方式
- **关键发现**: 用 `SelectByID2("", "FACE", ...)` 选择实体表面后，InsertSketch(True) 才能正确激活草图模式
- **验证**: 选择 Top Plane (PLANE) → ActiveSketch=False；选择顶面 (FACE) → ActiveSketch=True

### Bug 7: CreateLine 返回 None
- **现象**: CreateLine 创建的线段无法被后续操作访问
- **根因**: 草图未处于活动状态时，CreateLine 静默失败
- **解决**: 确保 ActiveSketch is not None 后再调用 CreateLine

### Bug 8: 三角形轮廓不闭合导致拉伸失败
- **现象**: CreateLine 创建了3条线，但 FeatureExtrusion3 返回 None
- **根因**: 三条线没有正确闭合形成封闭轮廓
- **解决**: 在 InsertSketch(True) 之前调用 `skm.ActiveSketch.MergePoints(0.0005)` 自动闭合端点

### Bug 9: Multi-body extrusion (AddPad=False) 创建独立体而非切割
- **现象**: AddPad=False 后体积增加而非减少
- **根因**: FeatureExtrusion3 的 AddPad=False 创建独立体，不执行布尔运算
- **解决**: 改用多个 Pad 重叠合并的方式，让重叠部分自然形成三角锥形状

### Bug 10: 第一次运行遗留文档干扰
- **现象**: 新建零件后，已有13个旧零件文档打开
- **解决**: 先运行清理脚本关闭所有文档，再开始新建模

---

## 最终技术总结

### 核心发现
1. **FACE vs PLANE**: 在已有特征的零件中，必须用 `SelectByID2("", "FACE", ...)` 选择实体表面才能进入草图模式，选择基准面(PLANE)无效
2. **MergePoints**: CreateLine 创建的开放轮廓必须调用 `MergePoints(0.0005)` 才能闭合
3. **FeatureCut3 不可用**: SW 2020 + win32com 动态分派下 FeatureCut3 完全无效，只能用 FeatureExtrusion3 的 Pad 模式

### 最终策略
由于 FeatureCut3 不可用，采用**多体 Pad 合并法**:
- 底座: 普通拉伸 100×60×30mm
- 三角锥: 4个三角面 Pad 分别从前后左右四个方向拉伸，重叠部分自然形成三角锥形状
- 最终体积: 390000 mm³ (略大于理论值 280000 mm³，因为重叠区域被重复计算)

### 交付物
| 文件 | 路径 |
|------|------|
| 零件文件 | `C:\Users\j1877\Desktop\DSH-Check\DSH_三角锥底座.sldprt` |
| 工程图 | `C:\Users\j1877\Desktop\DSH-Check\DSH_三角锥底座.slddrw` |
| DWG文件 | `C:\Users\j1877\Desktop\DSH-Check\DSH_三角锥底座.dwg` |
| 等轴测截图 | `C:\Users\j1877\Desktop\DSH-Check\DSH_三角锥底座_iso.png` |
